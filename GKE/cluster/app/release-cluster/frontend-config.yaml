apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: frontend-frontend-config
spec:
  sslPolicy: boutique-ssl-policy
  redirectToHttps:
    enabled: true
    responseCodeName: ""